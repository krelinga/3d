# Build output. Meshes are derived, not source -- see docs/design/initial-design.md.
out/
build/
thumbnails/**/.tmp-*

# Live preview server scratch.
viewer/.cache/

# Python bytecode from the tools running inside the container.
__pycache__/
*.pyc
