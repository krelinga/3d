// Unit constants.
//
// OpenSCAD is unitless; this repo treats one unit as one millimetre, which is
// what 3MF records and what every slicer expects. `inch` exists because the
// bases are specified in imperial sizes.
inch = 25.4;
